<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".PushLatencyApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.PushLatency">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".messaging.PushMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Diagnostic-only: see experiment.BaseExperimentService kdoc. -->
        <service
            android:name=".experiment.LongRunningTestService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="background-execution-limit-diagnostic" />
        </service>

        <!-- Diagnostic-only: see experiment.DataSyncTestService kdoc. -->
        <service
            android:name=".experiment.DataSyncTestService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Diagnostic-only: see experiment.AlwaysOnBackgroundService kdoc. -->
        <service
            android:name=".experiment.AlwaysOnBackgroundService"
            android:exported="false" />

    </application>

</manifest>
